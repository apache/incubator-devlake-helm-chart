apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "devlake.fullname" . }}-test-connection"
  labels:
    {{- include "devlake.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "devlake.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
